// Drizzle schema for appee.
// Tables defined here are managed via drizzle-kit (db:generate / db:push).
// The empty export keeps the file a module while the first table is being
// designed in 2.B.
export {};
